# Where the social feed API is running. Vite only exposes variables that start
# with VITE_ to the browser, which is why the name looks like this.
#
# In production this is the deployed API, e.g. https://your-api.onrender.com
VITE_API_URL=http://localhost:5006
